<%  // this is a file full of interfaces, lets render each one %>

<% for ( let i=0; i < schemas.length; i++) { %>
  <%- await convertJsonSchemaToTypescript(schemas[i]) %>
<% } %>

<% // code that uses the interfaces goes here %>